services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: docudiff
      POSTGRES_PASSWORD: docudiff
      POSTGRES_DB: docudiff
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U docudiff -d docudiff"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://docudiff:docudiff@db:5432/docudiff
      UPLOAD_DIR: /app/uploads
      # In-container localhost is not the Windows host. Override this only when needed.
      OLLAMA_BASE_URL: "${OLLAMA_DOCKER_BASE_URL:-http://host.docker.internal:11434}"
    ports:
      - "8001:8000"
    volumes:
      - ./uploads:/app/uploads
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
